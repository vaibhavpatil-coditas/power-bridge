package com.coditas.powerbridge.repository;

import com.coditas.powerbridge.entity.MeterServiceProvider;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MeterServiceProviderRepository extends JpaRepository<MeterServiceProvider, Long> {
}
